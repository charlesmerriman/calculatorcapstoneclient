import type React from "react"
import { UnderConstruction } from "./UnderConstruction"

/** Public Changelog page (route: /changelog). Placeholder until real entries exist. */
export const Changelog: React.FC = () => <UnderConstruction title="Changelog" />
